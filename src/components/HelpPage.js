import React from 'react';
import { Link } from 'react-router-dom';

const HelpPage = () => (
  <div>
    Help Page, so easy no one will need it...<br/>
    <Link to="/">Home</Link>
  </div>
);

export default HelpPage;